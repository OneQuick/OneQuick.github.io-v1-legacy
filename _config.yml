domain: https://onequick.org
permalink: /404.html

collections:
  docs:
    output: true

defaults:
  - scope:
      path: ''
      type: docs
    values:
      layout: docs

exclude: ['serve.bat', 'generate.bat']

stable-version: "1.2.3"

plugins:
  - jekyll-sitemap
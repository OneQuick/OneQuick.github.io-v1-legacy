domain: https://onequick.org
permalink: /404.html

collections:
  docs:
    output: true

defaults:
  - scope:
      path: ''
      type: docs
    values:
      layout: docs

exclude: ['*.bat', ".shellcommand.yaml"]

stable-version: "1.2.5"

plugins:
  - jekyll-sitemap